<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Typing Test Result</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background: #f4f4f4;
            margin: 0;
            padding: 0;
        }
        .result-container {
            background: #fff;
            max-width: 400px;
            margin: 60px auto;
            padding: 30px 40px;
            border-radius: 8px;
            box-shadow: 0 2px 8px rgba(0,0,0,0.1);
            text-align: center;
        }
        .result-container h1 {
            margin-bottom: 20px;
            color: #333;
        }
        .result {
            font-size: 1.2em;
            margin: 15px 0;
        }
        .btn {
            display: inline-block;
            margin-top: 25px;
            padding: 10px 24px;
            background: #0078d7;
            color: #fff;
            border: none;
            border-radius: 4px;
            text-decoration: none;
            font-size: 1em;
            cursor: pointer;
        }
        .btn:hover {
            background: #005fa3;
        }
    </style>
</head>
<body>
    <div class="result-container">
        <h1>Typing Test Result</h1>
        <div class="result">
            <strong>Words Per Minute (WPM):</strong> <%= evaluation.wpm %>
        </div>
        <div class="result">
            <strong>Accuracy:</strong> <%= evaluation.wordAccuracy %>%
        </div>
        <div class="result">
            <strong>Time Taken:</strong> 60 seconds
        </div>
        <a href="/typetest" class="btn">Try Again</a>
    </div>
</body>
</html></div>